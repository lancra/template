{
  "executions": [
    {
      "display": "Resolving solution conflict.",
      "stage": "Pick",
      "commands": [
        "$repository/.template/scripts/resolve-conflict.ps1 -Path *.slnx -Ours 0 -Theirs 1",
        "git cherry-pick --continue --no-edit"
      ]
    }
  ]
}
